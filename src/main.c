#include <stddef.h>
#include "main.h"
#define _USE_MATH_DEFINES
#include "math.h"

#define tol 1e-10

long double recpower(long double base, long int exp) {

	
}

// Instruções para o Assignment

/*
Neste assignment você deve construir a função recpower, que
é uma versão recursiva da exponenciação (base^expoente).
Para a função, considere que o expoente pertence aos 
números inteiros e a base pertence aos reais. O resultado 
pertence aos reais.
O código inicial da função é dado logo acima, onde as entradas 
são: base, que é um long double, e exp, que é um long int. A 
saída da função é pelo retorno da mesma, que é um long double.

*/